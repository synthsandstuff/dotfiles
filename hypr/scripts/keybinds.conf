$mod=SUPER
$mod_shft=SUPER_SHIFT

# App shortcuts
bind=$mod, T, exec, alacritty
bind=$mod, space, exec, rofi -show drun -show-icons
bind=$mod, F, exec, firefox
bind=$mod, D, exec, vesktop 
bind=$mod, S, exec, spotify
bind=$mod, Q, killactive
bind=$mod, M, exit

# Move focus
bind=$mod, left, movefocus, l
bind=$mod, right, movefocus, r
bind=$mod, up, movefocus, u
bind=$mod, down, movefocus, d

# Move window
bind=$mod_shft, left, movewindow, l
bind=$mod_shft, right, movewindow, r
bind=$mod_shft, up, movewindow, u
bind=$mod_shft, down, movewindow, d

# Workspaces
bind=$mod, U, movetoworkspace, special
bind=$mod_shft, U, togglespecialworkspace

# Extra
bind=$mod_shft, S, exec, grim -g "$(slurp)" - | wl-copy 
bindel=,XF86AudioRaiseVolume, exec, --no-startup-id pactl set-sink-volume @DEFAULT_AUDIO_SINK@ +5%
bindel=,XF86AudioLowerVolume, exec, --no-startup-id pactl set-sink-volume @DEFAULT_AUDIO_SINK@ -5%
bindl=,XF86AudioPlay, exec, --no-startup-id playerctl play-pause
bindl=,XF86AudioPrev, exec, --no-startup-id playerctl previous
bindl=,XF86AudioNext, exec, --no-startup-id playerctl next
